/.github
/.mcp.json
/opencode.json
/.opencode
.DS_Store
/artifacts/
# dist is tracked for npm publish (package.json files: ["dist"]); build with `bun run build`
# /dist  <- intentionally not ignored; keep dist/index.js in git for direct install
node_modules/
package-lock.json
/.code-review-graph